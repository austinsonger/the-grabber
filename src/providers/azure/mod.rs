pub mod factory;

// Azure collector submodules go here, e.g.:
//   pub mod activity_log;      // → CloudTrail equivalent
//   pub mod defender;          // → SecurityHub / GuardDuty equivalent
//   pub mod entra_id;          // → IAM equivalent
//   pub mod key_vault;         // → KMS / SecretsManager equivalent
//   pub mod storage;           // → S3 equivalent
//   pub mod virtual_machines;  // → EC2 equivalent
//   pub mod policy;            // → AWS Config / SCPs equivalent
